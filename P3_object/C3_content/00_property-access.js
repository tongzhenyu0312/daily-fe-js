/**
 * 内容
 * 对象的内容是由 存储在特定命名位置 和 任意类型的值 所组成
 * 与对象属性的名称不同，对象的值 通常不在对象的存储容器内
 * JS中对象的键名都是字符串
 */

/**
 * 访问
 * 对象的属性的值的访问一般有两种方式
 * 1. 属性访问
 * 2. 键访问
 * 区别在于：
 * 1. 键访问能够不遵循 标识符规范(因为键名都是字符串)
 * 2. 键访问的键名 能够通过构造生成(键名可以随意搞，赋值，取反...)
 */